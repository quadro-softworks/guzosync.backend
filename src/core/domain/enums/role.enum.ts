export enum Role {
  Passenger = 'PASSENGER',
  QueueRegulator = 'QUEUE_REGULATOR',
  BusDriver = 'BUS_DRIVER',
  ControlCenter = 'CONTROL_CENTER',
  User = 'USER', // Generic user role often useful as default
}
