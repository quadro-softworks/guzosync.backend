PORT=3000
NODE_ENV=development
MONGO_URI=
API_BASE_PATH=/api/v1

JWT_SECRET=your_super_secret_random_string_here # Replace with a real secret
BCRYPT_SALT_ROUNDS=10 # Cost factor for hashing

# Example using generic SMTP - replace with your provider's details
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587 # Or 465 for SSL
EMAIL_SECURE=false # Use true for port 465
EMAIL_USER=your_email_username
EMAIL_PASS=your_email_password
EMAIL_FROM='"Your App Name" <no-reply@yourapp.com>' # Sender address

# Example using Ethereal (for testing without a real SMTP)
# Generate credentials at https://ethereal.email/create
# EMAIL_HOST=smtp.ethereal.email
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=your_ethereal_user@ethereal.email
# EMAIL_PASS=your_ethereal_password
# EMAIL_FROM='"Test App" <no-reply@example.com>'